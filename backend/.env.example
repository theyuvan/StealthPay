# Testnet relayer keypair — this account pays Soroban fees (register_proof, announce).
# Fund it at https://friendbot.stellar.org before first use.
RELAYER_SECRET=S...your_testnet_secret_key_here...

# Port (Railway sets this automatically — do not set manually on Railway)
PORT=4000
